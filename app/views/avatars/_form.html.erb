<%= form_for(@avatar, :html => { :multipart => true }) do |f| %>
  <% if @avatar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@avatar.errors.count, "error") %> prohibited this avatar from being saved:</h2>

      <ul>
      <% @avatar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
  	<%= f.label :flag,  :class => "control-label" %>
  	<div class="control">  	    
    <%= select_tag 'avatar[flag]', options_for_select([['Feminino', 1], ['Masculino', 2]]) %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :image,  :class => "control-label" %>
    <div class="control">
    <%= f.file_field :image %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit %>
  </div>
<% end %>
